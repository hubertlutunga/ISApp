---
deployment:
  tasks:
    - export DEPLOYPATH=/home/invizfxg/public_html/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/find $DEPLOYPATH -mindepth 1 -maxdepth 1 ! -name .well-known -exec rm -rf {} \;
    - /bin/cp -R ./* $DEPLOYPATH